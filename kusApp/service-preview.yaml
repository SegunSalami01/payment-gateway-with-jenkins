apiVersion: v1
kind: Service
metadata:
  name: payment-gateway-service-preview
  namespace: test
spec:
  selector:
    app: payment-gateway
  ports:
  - port: 8082
    targetPort: 8082
    protocol: TCP
  type: ClusterIP